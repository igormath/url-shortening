import './StartSection.css';

const StartSection = () => {
    return (
        <section className="start">
            <h3 className="start__title">Boost your links today</h3>
            <button className="start__btn btn">Get Started</button>
        </section>
    );
};

export default StartSection;
